/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "www.bhpalmbeach.com",
      "storage.googleapis.com",
      "img.freepik.com",
    ],
  },
};

module.exports = nextConfig;
